name: TWITHUB / README.md

on:
  schedule:
    - cron: "0 */12 * * *"
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  checkit:
    runs-on: ubuntu-latest
    steps:
      - name: test variables
        run: |
          if \
            [ "${{ secrets.TS_PROJECT_ID != '' }}" = "true" ] \
            && [ "${{ secrets.TS_AUTH_TOKEN != '' }}" = "true" ]; \
          then \
            echo "::set-output name=is-initialized::'true'"; \
            echo "TakeShape secrets are set!"; \
            echo "*** YOU SHOULD DELETE .github/workflows/setup.yml! ***"; \
          else \
            echo "::set-output name=is-initialized::false"; \
            echo "TakeShape secrets are NOT set"; \
          fi

  build:
    runs-on: ubuntu-latest
    needs: [checkit]
    if: needs.checkit.outputs.is-initialized == 'true'
    steps:
      - name: check it out!
        uses: actions/checkout@v2

      - name: twitting...
        env:
          TS_PROJECT_ID: ${{ secrets.TS_PROJECT_ID }}
          TS_AUTH_TOKEN: ${{ secrets.TS_AUTH_TOKEN }}
        run: |
          test -d static || mkdir static
          npx --package @takeshape/cli -c "tsg build"

      - name: substing...
        uses: mshick/fast-envsubst@v1
        with:
          in-file: README.md
          out-file: README.md

      - name: great job!
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "$(git --no-pager log --format=format:'%an' -n 1)"
          git config user.email "$(git --no-pager log --format=format:'%ae' -n 1)"
          git pull --ff-only \
          && git add -A \
          && git diff HEAD --quiet --exit-code \
          || git commit -am "great job!" \
          && git push
